# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

# UI Kit

Use **gluestack-ui** (UniWind) as the default UI kit. See `.cursor/rules/gluestack-ui.mdc` and https://gluestack.io/ui/docs.
